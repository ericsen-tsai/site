name: Release

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write
  pull-requests: write

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node.js for NPM Trusted Publishing
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          registry-url: "https://registry.npmjs.org"
          scope: "@erichandsen"

      # Pin npm 11.x: OIDC needs >=11.5.1, but npm 12 hard-errors on
      # --git-checks that changeset→pnpm@10.6 still forwards during publish.
      - name: Install npm 11 (for OIDC support)
        run: npm install -g npm@11

      - name: Verify npm authentication
        shell: bash
        run: |
          echo "Checking npm authentication..."
          cat ~/.npmrc || echo "No .npmrc found in home directory"
          npm whoami || echo "Not authenticated"

      - name: Setup pnpm cache & Install dependencies
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV
          pnpm install --frozen-lockfile

      - name: Create Release Pull Request or Publish to NPM
        id: changesets
        uses: changesets/action@v1
        with:
          commit: "chore(release): version packages"
          title: "chore(release): version packages"
          version: pnpm run version
          publish: pnpm run release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
